# CLAUDE.md

This file gives Claude (or any AI assistant working on this repo) persistent context about the project. Keep it up to date as decisions change — it should always reflect *current* reality, not history (history goes in `plan.md` or commit messages).

---

## Project Summary

A farm management platform, starting with irrigation calculations and growing into full farm operations: assets/equipment, crop records, tasks, finance, reporting.

- **Users:** for now, multiple **managers** with full access to everything — owner/worker as distinct roles exist in the schema and UI but aren't used yet. Worker-specific restricted views and access are deferred to a later phase (see plan.md)
- **Deployment today:** local PC on the farm, served over LAN
- **Deployment path:** Docker Compose next → cloud later (same containers, different environment config)
- **Architecture style:** modular monolith — one deployable app, strict internal boundaries between domain modules, shared core platform

Full rationale and diagrams live in `docs/design.md` (the original high-level design doc). This file is the quick-reference for day-to-day/AI-assisted work; that one is the "why."

---

## Architecture Rules (don't violate these)

1. **Modules don't reach into each other's data directly.** A module talks to another module only through its public service/API layer, never by importing another module's internal repository/DB access code directly.
2. **All spatial references go through Core's Farm → Field/Block → Zone registry.** Don't let a module (e.g. Irrigation) invent its own notion of "location" — it references Core's Zone entity.
3. **Each module owns its own DB schema namespace** (e.g. `irrigation.*`, `assets.*`) and its own migrations. Don't create cross-schema foreign keys where avoidable — prefer referencing by ID and enforcing at the application layer.
4. **Calculation logic is a pure library, no I/O.** E.g. the irrigation ET₀/water-demand calculations must not depend on the DB or HTTP layer, so they stay independently testable.
5. **New feature = new module**, added via the module registry, not by bolting routes onto an existing module.
6. **No shortcuts that block the cloud migration path.** E.g. don't hardcode local filesystem paths outside the storage abstraction, don't assume LAN-only auth flows.

---

## Tech Stack (current)

| Layer | Choice |
|---|---|
| Backend | Python, FastAPI |
| Frontend | Blazor WebAssembly (C#), modules as Razor Class Libraries, lazy-loaded per route |
| Database | PostgreSQL, one schema per module |
| ORM/migrations | SQLAlchemy (2.x, ORM + Core) + Alembic for migrations |
| Auth | JWT sessions (`pyjwt` + `passlib`/`bcrypt`), roles stored in DB (`core.users.role`: owner/manager/worker); 12h token lifetime, no refresh tokens |
| Background jobs | APScheduler in-process (upgrade to Celery + Redis only if justified) |
| Packaging | Docker Compose: `app`, `db` |
| Reverse proxy | Caddy |
| Localization | `Microsoft.Extensions.Localization` (resx-based `IStringLocalizer<T>`), English default + Afrikaans (`af`); persisted per-user via `core.users.preferred_language` (`PATCH /api/v1/core/users/me/language`), with browser `localStorage` as the fast local cache driving the synchronous pre-render culture bootstrap |

> Prisma's Python client was considered but is unmaintained (deprecated March 2025, following Prisma's core rewrite from Rust to TypeScript) — not used for that reason.

> Localization currently covers only `Farm.Web.Core` chrome (nav, shell, login, dashboard) — `Farm.Web.Irrigation`'s `RunCalculator.razor` (~300 hardcoded strings across 27 calculators) is explicitly deferred to a future pass and has zero localization touchpoints today. Requires `<SatelliteResourceLanguages>en;af</SatelliteResourceLanguages>` and `<BlazorWebAssemblyLoadAllGlobalizationData>true</BlazorWebAssemblyLoadAllGlobalizationData>` in `apps/web/Farm.Web.Host.csproj` — the latter is required for Blazor WASM to allow switching `CurrentCulture`/`CurrentUICulture` away from the build-time default at runtime (confirmed by testing; omitting it throws "Blazor detected a change in the application's culture..."). Calculator numeric formatting is unaffected regardless of UI language since `RunCalculator.razor` always parses/formats with an explicit `CultureInfo.InvariantCulture`.

> Frontend is C#/Blazor WASM rather than React so the whole stack stays statically typed and each feature module can be a real compiled unit (Razor Class Library) rather than just a folder convention. Trade-off accepted: true drop-in-without-rebuild plugin loading is harder in .NET's assembly model than in JS — modules are lazy-loaded at runtime but still need to be referenced by the host project at build time.

> Auth: `passlib` (unmaintained since 2020) breaks with `bcrypt` ≥4.1 (probes a removed `__about__` attribute) — pin `bcrypt==4.0.1` explicitly alongside `passlib` rather than `passlib[bcrypt]`'s unpinned extra. The only way to create a user today is the CLI seed script (`python -m app.core.seed_admin`, in `apps/api/`) — no signup/registration endpoint exists; account creation UI is a future in-app admin screen. JWT session lifetime is 12 hours with no refresh token (`config.py`'s `jwt_expiry_minutes=720`) — an accepted v1 simplification for the LAN/low-risk deployment model. `require_role(*roles)` in `app/core/auth.py` is the reusable FastAPI dependency future modules should import for role-gating their own routers.

> **Role usage today vs. schema:** the schema/JWT/UI support three roles (owner/manager/worker), and that scaffolding stays as-is in the code. In practice, for now and until a later phase, only **manager** is used — multiple manager accounts, each with full access to everything (equivalent to what "owner" would mean). Worker accounts, the restricted Worker dashboard view, and any real owner-vs-manager distinction are deferred (see plan.md Parking Lot) — no code changes were made for this scoping decision. `Dashboard.razor`'s existing role-driven logic (owner/manager → full view + toggle, worker → locked-down view) already matches this model unmodified. The CLI seed script still hardcodes `Role.OWNER` for the account it creates, which behaves identically to a manager under the current logic — revisit whether to reseed as `Role.MANAGER` (or drop the distinction) once the manager-only account-creation UI is built.

> **Farm/Field/Zone registry (Core):** `core.farms`/`core.fields`/`core.zones` tables (plain intra-schema FKs, fine per rule 3 — only *cross-schema* FKs are discouraged). Single `Farm` row seeded via the migration itself (`op.bulk_insert`, fixed UUID `1111...`, name "Ridgeline Farm"). `area_ha` is `Float`/`double` end-to-end (not `Decimal`/`Numeric`) to match the existing calculators. `Zone.status` is now really computed (see the "Real Zone.status" callout below) — no longer a hardcoded placeholder. Soft-delete only (`is_active=False` via `POST .../archive`), never a real `DELETE`. Frontend lives in `Farm.Web.Core/Registry/` (not `Farm.Web.Core/Farm/` — see gotcha below) with pages at `/irrigation/zones` (fills the previously-dead NavMenu link), `/farm/fields` (Farm Structure), `/farm/fields/{id}/edit`, `/farm/zones/{id}/edit`.
>
> **Three real bugs found only by running this, not caught in planning:** (1) `alembic/env.py`'s `context.configure(...)` was missing `include_schemas=True` in both online/offline modes — without it, autogenerate never reflects the `core` schema when diffing, so it silently thinks *every* `core.*` table is new on every single autogenerate run (already-applied tables included). This was latent since the very first migration; only became visible once a second core-schema migration was generated. Now fixed — required for any future Core migration to autogenerate correctly. (2) Naming a new C# namespace `Farm.Web.Core.Farm` collides with the project's own root namespace `Farm.Web.Core` (the compiler can't resolve subsequent `Farm.Web.Core.*` usings once `Farm` also resolves to the new namespace) — renamed to `Farm.Web.Core.Registry`. Don't name a nested namespace segment after the first segment of the root namespace. (3) System.Text.Json's default `PostAsJsonAsync`/`GetFromJsonAsync` serialization is camelCase (`areaHa`), but FastAPI/Pydantic expects snake_case (`area_ha`) — every multi-word DTO property needs an explicit `[JsonPropertyName("snake_case")]` attribute (see `Registry/FarmModels.cs`). Also: no mechanism existed yet to attach the JWT to outgoing typed-HttpClient requests (`AuthService` never needed one — login is anonymous) — added `Auth/AuthTokenHandler.cs`, a `DelegatingHandler` reading the token via `farmAuth.get` JS interop and setting the `Authorization: Bearer` header, wired via `.AddHttpMessageHandler<AuthTokenHandler>()` on the `IFarmService` typed client. Any future authenticated typed client needs the same handler attached.
>
> **Irrigation: `CalculationRun` logging (no server-side math).** First non-Core module (`app/irrigation/`, own `irrigation` schema). `CalculationRun` stores `zone_id` (plain `uuid.UUID`, no FK — cross-schema, validated at the app layer via `app.core.service.get_zone`, per rule 3), `calculator_type` (enum values `et0-run-time`/`manual-volume`/`drip-emitter`/`set-time`, matching the frontend calculator IDs 1:1), and `inputs`/`outputs` (`JSONB`) stored verbatim. **By explicit user decision this module has no `engine.py`/calculation logic at all** — all math stays in the client-side `Farm.Irrigation.Calculators` C# library; the backend only validates the zone and persists whatever the frontend already computed. `POST`/`GET /api/v1/irrigation/calculation-runs` require only `get_current_user` (any role) — logging a run isn't a structural mutation like Field/Zone create, so no `require_role` gate. Frontend: `RunCalculator.razor` reads `?zone={id}` (shows a "Logging against: {name}" banner) or falls back to a zone `<select>` when absent; the four calculators with a "Confirm & Log Run" button now actually log. **Gotcha:** `Farm.Web.Irrigation` is lazy-loaded — any service a lazy module's components inject via DI must have its interface/implementation live in an *eagerly*-loaded assembly (`Farm.Web.Core`), never in the lazy module itself, or `Program.cs`'s eager DI registration forces the lazy assembly to load too early and throws "Could not load file or assembly" on every page. `IIrrigationService`/`IrrigationService` accordingly live in `Farm.Web.Core/Irrigation/`, not in `Farm.Web.Irrigation`.
>
> **Per-user language persistence + Core: Settings.** `core.users.preferred_language` (default `"en"`, backfilled via migration `server_default`) — `LanguageService.SetLanguageAsync` now PATCHes `/api/v1/core/users/me/language` in addition to `localStorage` (best-effort, wrapped in try/catch so an offline/failed PATCH never blocks the client-side switch). On login, `AuthService.LoginAsync` fetches `/auth/me` (needs the token already stored) and syncs `localStorage`'s `farm-language` to the user's saved preference *before* the existing forced full-page reload — this is how a per-user preference reaches the synchronous pre-render culture bootstrap in `Program.cs` without that bootstrap itself needing a network call. `LanguageService`'s DI registration changed from `AddScoped` to a typed `AddHttpClient<ILanguageService, LanguageService>().AddHttpMessageHandler<AuthTokenHandler>()` (same pattern as `IFarmService`). `Core: Settings` is a single-row `core.settings` table (`timezone`, `kc_establishment`/`kc_midseason`/`kc_lateseason` — defaults copied verbatim from `Farm.Irrigation.Calculators/Presets.cs`'s `CropCoefficients`, seeded via the migration itself, same fixed-UUID pattern as `Farm`). `GET /settings` any authenticated role, `PATCH /settings` manager/owner-gated. Frontend: `FarmSettings.razor` at `/farm/settings`, reachable via a "Farm Settings" button on Farm Structure (same not-a-nav-item pattern as Farm Structure itself). **Wiring the calculators to actually consume these Kc defaults is explicitly out of scope** — `Presets.cs`'s hardcoded constants are untouched; that's a separate follow-up.
>
> **Kc-defaults wiring + Calculation History UI.** Both frontend-only, no backend changes. `RunCalculator.razor`'s calculator catalogue (`_categories`) was `static readonly`, embedding the hardcoded `CropCoefficients` growth-stage choices directly — converted to a private static method `BuildCategories(IReadOnlyList<Choice> growthStages)` plus an instance field seeded with the hardcoded defaults, so `OnInitializedAsync` can rebuild it from `FarmService.GetSettingsAsync()`'s real Kc values once fetched (falling back to the hardcoded defaults if Settings is unreachable — same null-safe style as the zone lookups already in that file). Only ET₀ Run-Time and Crop Water Need use the Kc dropdown; the other 25 calculators are unaffected. `Presets.cs`'s `CropCoefficients` constants remain untouched as the fallback. New `CalculationHistory.razor` (`Farm.Web.Core/Pages/`, not `Farm.Web.Irrigation` — same precedent as `ZoneList.razor`) at `/irrigation/history`, fills the previously-dead NavMenu sub-item: zone-scoped via `?zone={id}` or a `<select>` fallback (mirrors `RunCalculator.razor`'s exact zone-resolution pattern), renders each `CalculationRun` generically (`inputs`/`outputs` as comma-joined `key: value` pairs — not calculator-specific formatting, flagged as future polish) with a small local dictionary mapping the four known `calculator_type` values to the same display titles used in `RunCalculator.razor`. Reachable via a new "View history" link on each `ZoneList.razor` card (`@onclick:stopPropagation`/`@onclick:preventDefault` so it doesn't also trigger the card's own Run Calculator navigation).
>
> **Real Zone.status via manual per-zone irrigation interval.** `core.zones.irrigation_interval_days` (nullable `Integer`, set optionally in `ZoneForm.razor` as "Water every N days") — if unset, status stays `"on-schedule"`. If set, `core.service._compute_zone_status(interval_days, last_activity_at)` compares days elapsed since the zone's most recent logged `CalculationRun` (or `zone.created_at` if never run) against the interval: under it → `on-schedule`, at/past it but within a fixed `STATUS_DUE_GRACE_DAYS = 3` → `due`, beyond that → `overdue`. The grace constant is intentionally hardcoded, not Settings-configurable (explicit decision). **Gotcha, same shape as the Farm.Web.Irrigation one above:** computing this needs Core to read Irrigation's `CalculationRun` data — an inversion of the normal dependency direction. `core/service.py`'s `_last_run_lookup` does a **function-scoped (deferred) import** of `app.irrigation.service` rather than a top-level one, since `irrigation/service.py` already imports `core/service.py` at module level (a top-level reverse import would be a real circular import). New `app.irrigation.service.get_last_run_at_by_zone(db, zone_ids)` batches the "most recent run per zone" lookup for `list_zones` in one grouped query, avoiding N+1. ~~Auto-computed intervals remain future work~~ — the real `Schedule` model now exists (see the Schedule callout below); the manual field survives as its fallback.
>
> **Irrigation: real `Schedule` (agronomic watering interval).** `irrigation.schedules` — one row per zone (unique no-FK `zone_id`, app-validated): four manual agronomic inputs (soil available-water mm/m, root depth m, allowable depletion %, peak crop use mm/day) plus the **client-computed** `interval_days`/`readily_available_water_mm` stored **verbatim** (no-server-math rule — the math is the already-tested `SchedulingCalculators.MaxIrrigationInterval` in the lazy calculators lib). `PUT`/`GET /api/v1/irrigation/zones/{id}/schedule` (upsert, any authenticated role; GET → 200+null when unset). **Status:** `core/service.py`'s `_schedule_interval_lookup` (same deferred-import inversion as `_last_run_lookup`) supersedes the manual `zone.irrigation_interval_days` with floor(schedule interval) when a schedule exists — `_compute_zone_status` unchanged, no-schedule zones behave exactly as before. Frontend: `IScheduleStore`/`HttpScheduleStore` in **eager Core** (ZoneOverview reads it — same DI rule as the design store), editor `ZoneSchedule.razor` in the **lazy** module (it calls `SchedulingCalculators`, which must not be referenced from Core) with live recompute + next-due (last run ?? zone creation + interval), and a real "Irrigation schedule" card on Zone Overview (EN+AF). Weather-driven self-advancing water balance still future (Phase 1.5); `WaterSource`/`IrrigationSystem` entities still unbuilt. **Zone Designer untouched by this feature** (explicit constraint — regression-verified).
>
> **Zone Overview landing page.** New `ZoneOverview.razor` (`Farm.Web.Core/Pages/`) at `/irrigation/zones/{Id:guid}`, sitting between `ZoneList.razor` and the calculator flow per a UI mockup. Frontend-only, no backend changes — "last watered" is derived client-side from `IIrrigationService.ListCalculationRunsAsync(zoneId)` (already newest-first; take the first entry, or a literal "Never" if empty), not a new DTO field. The "Reports and history" card is a **real** link into `/irrigation/history?zone={id}` (deliberate choice — the mockup showed an inert placeholder there, but hiding an already-working feature made no sense); the "Irrigation design" card stays a literal static placeholder since no real design/canvas feature exists. `ZoneList.razor`'s card click now goes here (`GoToOverview`) instead of straight into the calculator (`GoToRunCalculator` removed as dead code); `RunCalculator.razor`'s `BackToPicker()` is now zone-aware — when arrived via `?zone=` (i.e. from this page's "Do calculations" button), the back button (on both the picker screen and from inside a selected calculator) navigates back here instead of resetting to the in-page calculator grid, with the label switching between "← Back to zone overview" and the original "← Choose a different calculator" accordingly; the non-zone-scoped flow is unchanged. "Zone Designs" was a dead NavMenu link at the time this landed — no longer true, see the Zone Design callout below.
>
> **Zone Design (irrigation layout designer).** Two new pages in the lazy `Farm.Web.Irrigation` module (no host changes needed — `App.razor`'s lazy-load guard already covers all `/irrigation/*` paths): `ZoneDesigns.razor` at `/irrigation/zone-designs` (fills the previously-dead NavMenu link — zone-card grid with a "Design saved"/"No design yet" flag, card click → Zone Overview) and `ZoneDesigner.razor` at `/irrigation/zones/{id}/design` (SVG canvas + toolbar for drawing main/lateral pipes and placing pump/sprinklers/valves/water source, selected-element inspector, 3-mode solver, live hydraulics results). **Math is the existing engine** — `HydraulicsCalculators`/`ApplicationCalculators` reused directly (unit gotcha: hydraulics methods take **m³/h** but the designer's inputs are L/min — convert `× 0.06` at the call site) plus a small new `ZoneDesignCalculators.cs` (demand flow, max-sprinklers inverse, coverage ratio, runtime-from-rate) with xunit tests pinning the reference mockup's numbers. Friction = Hazen-Williams (C=150) with **per-segment flow accumulation on the main** (split at each connected lateral's take-off — `DesignPipe.DistanceAlongTo`; inlet end = nearest placed pump/source, else draw start; full-demand fallback when nothing routes) + the busiest lateral's Christiansen loss, **assuming 3 laterals when none are drawn** (documented, surfaced in the UI); required pressure = friction + the **editable** `SprinklerHeadPressureBar` input (default 2.0 — initializer doubles as JSON back-compat), with a visible "planning estimates" caveat under Results (elevation/fitting losses not modelled). **Designs are backend-persisted as immutable versions** (`irrigation.zone_design_versions` — JSONB payload stored verbatim, no server-side interpretation, consistent with the module's no-engine rule): edits stay **local-only** until an explicit Save creates a new version (that's the playground mode — refresh discards unsaved work by design), Discard reloads the latest, and a History list loads any older version as a dirty working copy ("restore by saving"). `IZoneDesignStore` (GetLatest/SaveVersion/ListVersions/GetVersion/HasDesign) + `HttpZoneDesignStore` typed client live in `Farm.Web.Core/Irrigation/` — Core, not the lazy module, because `ZoneOverview.razor` reads design status (same eager-assembly DI gotcha as `IIrrigationService`). The `ZoneDesign` payload round-trips as client-serialized camelCase JSON (only `DesignVersionInfo`'s snake_case wrapper fields need `[JsonPropertyName]`); **the Design-Inputs rows' lambdas must read `_design.Inputs` dynamically, not capture the instance** — Load/Discard swaps the working design object. `ZoneOverview.razor`'s "Irrigation design" card is now a real link into the designer and its details row computes "Design saved"/"Not started" (`ZoneOverview_IrrigationDesign_Saved`, EN+AF). SVG technique (greenfield — no prior SVG in the app, **no JS interop needed**): viewBox in metres at 9 px/m with `vector-effect="non-scaling-stroke"`; placement clicks land on a full-size background `<rect>` whose `MouseEventArgs.OffsetX/Y ÷ 9` gives metres (snapped to 1 m); elements select via their own `@onclick` + `stopPropagation`; pipe drawing is click-to-place vertices, axis-constrained to horizontal/vertical, double-click finishes; **first pipe drawn = main** but the pipe inspector's "Make main pipe"/"Make this a lateral" toggle reclassifies after the fact (single-main invariant enforced; no-main state falls back to the "No main pipe" warning). Selected pipes show **endpoint resize handles** that slide along the adjacent segment's axis (1 m snap, no collapse onto the neighbor); interior vertices are not editable yet. **Two Razor gotchas hit:** relational switch patterns (`< 0.9 =>`) inside a `@code` block break the Razor parser (`<` misread as a tag → RZ1006/RZ9980) — use plain ifs; and `[Authorize]` in this module needed `@using Microsoft.AspNetCore.Authorization` added to its `_Imports.razor`. Designer pages are unlocalized (module-wide deferral, same as RunCalculator). **Structure:** `ZoneDesigner.razor` is a thin adapter — interaction logic lives in `Farm.Web.Irrigation/Designer/ZoneDesignerSession.cs` (tools/selection/drawing/drag, metres in, `bool changed` out) and calculations in `Designer/ZoneDesignEvaluator.cs` (pure `Evaluate(ZoneDesign) → DesignEvaluation`), both unit-tested in `web-modules/Farm.Web.Irrigation.Tests/`. **These must stay in the lazy module, not Core:** the evaluator references `Farm.Irrigation.Calculators`, itself lazy-loaded — Core referencing it would force-load it eagerly and break lazy loading. Extend designer behavior by editing the session/evaluator + their tests, not by adding logic back into the .razor. **Canvas→math coupling:** sprinklers placed on the canvas override the "Sprinkler count" input (which then renders disabled with a "from canvas" note; it only applies while the layout has none); each placed sprinkler is assigned to its **nearest lateral** (`DesignPipe.DistanceToPoint`) so laterals carry their real flows, lateral friction uses `LateralFrictionLoss` (Hazen-Williams × Christiansen F), and results report the busiest lateral as the worst path. Elements are **draggable with the Select tool** (1 m snap; pipes translate whole; assignment re-evaluates on drop) — dragging works without JS interop via a transparent full-canvas overlay `<rect>` that keeps `OffsetX/Y` in SVG space (offset is target-relative, so reading it off element shapes mid-drag would break). Sprinklers (flow/radius), pipes (diameter), and the pump (rated flow/pressure) carry **nullable per-element overrides** edited in the inspector (null = inherit the Design Input; "Reset to design defaults" clears them; invalid/≤0 entry also clears) that feed demand, per-lateral hydraulics, main velocity/friction, application rate, coverage (avg effective radius) and the max-sprinklers solver; with ≥2 placed sprinklers, application rate/runtime/coverage also derive their **spacing from the layout** (mean nearest-neighbour distance, `ZoneDesignCalculators.MeanNearestNeighbourSpacing` — equals grid spacing on a uniform grid; the spacing input then disables "from canvas") — valve/water-source markers remain visual-only. The global Design Inputs reflect canvas overrides via `InputRow.Status` (disabled + "set on canvas"/"set on pump" when fully overridden, "k of n custom" when partial). **Connectivity is checked geometrically** (`DesignPipe.DistanceToPipe`): laterals >1 m from the main are flagged amber-dashed and carry no flow (sprinklers route to nearest *connected* lateral, nearest-any fallback when none are), sprinklers >2 m from their lateral render amber, and an amber warnings card lists the issues — still not a real pipe-network graph.
>
> Update this table immediately if a tech choice changes — this is the source of truth, not the design doc.

---

## Repo Structure (target)

```
/apps
  /web                       → Blazor WASM host app
    Program.cs               → app entrypoint, route table, module assembly registry
    /Shell                    → layout, nav, auth UI, shared shell components
    App.razor                 → root router, configured for lazy assembly loading
  /web-modules
    /Farm.Web.Core            → Razor Class Library: core UI (auth, farm/field/zone registry screens) — referenced eagerly by host
    /Farm.Web.Irrigation      → Razor Class Library: irrigation UI — lazy-loaded on route match
    /Farm.Web.Assets          → Razor Class Library: assets UI (Phase 2+) — lazy-loaded
    /Farm.Web.Crops           → Razor Class Library: crops UI (Phase 3+) — lazy-loaded
    ...
  /api                       → FastAPI backend
    /app
      main.py                → app entrypoint, module/router registration
      /core                  → auth, users, roles, farm/field/zone registry, notifications, audit log, file storage
        models.py             → SQLAlchemy models (schema: core)
        router.py             → FastAPI router
        service.py            → business logic
        schemas.py            → Pydantic request/response models
      /irrigation             → irrigation module (same internal shape as /core)
        models.py              → CalculationRun (logging only — see Tech Stack callout; no engine.py yet, by explicit decision)
        router.py
        service.py
        schemas.py
      /assets                 → assets module (Phase 2+)
      /crops                  → crops module (Phase 3+)
      ...
    /alembic
      versions/               → migration scripts, one history per module where practical (see Conventions)
      env.py
    alembic.ini
/docs
  design.md                   → original high-level design doc
  decisions/                  → one file per significant architecture decision (ADR-style), optional but recommended
/docker-compose.yml
CLAUDE.md
plan.md
```

---

## Conventions

- **API routes:** versioned, `/api/v1/<module>/...`, each module exposes a FastAPI `APIRouter` registered in `main.py` — don't add routes directly to the app object.
- **Module folder** (`app/<module>/`) contains its own `models.py` (SQLAlchemy models), `schemas.py` (Pydantic), `service.py` (business logic), and `router.py` (HTTP layer). Routers call services; services use models; nothing outside the module imports another module's `models.py` directly.
- **Database schema:** each module's tables live in their own Postgres schema (e.g. `irrigation.*`) — set via SQLAlchemy's `__table_args__ = {"schema": "irrigation"}` (or equivalent) on that module's models.
- **Frontend module = a Razor Class Library** under `/web-modules`, named `Farm.Web.<Module>`. It owns its own `.razor` pages/components, routes, and any module-specific state/services. Only `Farm.Web.Core` is referenced eagerly by the host; every other module is registered for **lazy loading** in `App.razor`'s router config so its assembly only downloads when a matching route is hit.
- **Frontend ↔ backend module naming stays aligned** — `Farm.Web.Irrigation` talks to `/api/v1/irrigation/...`, and so on. If you rename one, rename the other in the same change.
- **No cross-module component references** on the frontend beyond `Farm.Web.Core` — a feature module can use Core's shared components (layout, auth state, common UI) but not another feature module's components directly. Shared-but-not-core UI belongs in Core, not borrowed peer-to-peer.
- **Tests:** calculation-engine and service-layer logic gets unit tests (pytest); aim for coverage on anything that decides "how much water" or "when maintenance is due" — this is where real-world cost lives.
- **Commits:** reference the module and phase, e.g. `irrigation: add ET0 calculation engine (Phase 1)`.
- **Migrations:** single Alembic history for the whole project (Alembic doesn't natively do one-history-per-module) — but name revisions with the module prefix, e.g. `irrigation_add_calculation_run`, so the history stays readable as modules multiply. Never edit a migration that's already been applied anywhere outside local dev — write a new one.

---

## Current Status

> Update this section every session — it's the fastest way for an AI assistant (or future you) to know where things stand without re-reading the whole history.

- **Current phase:** Phase 1 — Foundation + Irrigation
- **What exists:** FastAPI backend (`apps/api`) with working **Core: Users + Auth** (JWT login/`/me`, `core.users` incl. `preferred_language`, roles, CLI seed script, CORS), **Core: Farm/Field/Zone registry** (`core.farms`/`fields`/`zones` — full CRUD + archive endpoints, manager/owner-gated mutations via `require_role`, single seeded Farm row), **Core: Settings** (single-row `core.settings` — timezone + Kc defaults, `GET` any role/`PATCH` manager/owner), and now **Irrigation: `CalculationRun` logging** (`app/irrigation/` — first non-Core module; logs zone-referencing calculation runs with no server-side math — see Tech Stack callout above for schema/gotchas on all four); Blazor WASM host (`apps/web`) + `Farm.Web.Core` RCL wired to real auth (`AuthorizeRouteView`/`[Authorize]`, `FarmAuthStateProvider` + `IAuthService`, working Login/Sign-out, role-driven Dashboard), the Farm registry UI (`Registry/IFarmService`+`FarmService`, Zone List at `/irrigation/zones` — no longer a dead link, Farm Structure + Field/Zone add/edit/archive forms + Farm Settings at `/farm/*`), and now `Irrigation/IIrrigationService`+`IrrigationService`; English/Afrikaans localization wired for all of the above chrome (not `RunCalculator.razor`, still deferred), switchable via a nav-bar language toggle, **persisted per-user** (synced from/to the backend on login and on switch, `localStorage` as the fast local cache); `Farm.Web.Irrigation` RCL (lazy-loaded, proves the plugin pattern) with 27 client-side calculators at `/irrigation/run-calculator` (formulas verified against irrigation.wsu.edu and watertankcalculator.com sources — see plan.md Decisions Log 2026-07-14), backed by the standalone zero-dependency `Farm.Irrigation.Calculators` C# library (metric units, 37 xunit tests) — reads the `?zone=` query param (banner or `<select>` fallback), the four loggable calculators (ET₀ Run-Time, Manual Volume, Drip Emitter, Set Time) log to the backend, and **ET₀ Run-Time/Crop Water Need now fetch their Kc dropdown values from `GET /api/v1/core/settings`** instead of the hardcoded `Presets.cs` constants (which remain the fallback default); still not localized. Also new: `CalculationHistory.razor` (`Farm.Web.Core/Pages/`) at `/irrigation/history` — fills the previously-dead NavMenu "History" link with a zone-scoped, read-only view of logged `CalculationRun`s, reachable via a "View history" link on each Zone List card. **`Zone.status` is now really computed** (no longer hardcoded "on-schedule") via an optional per-zone `irrigation_interval_days` set in `ZoneForm.razor` compared against the zone's most recent logged `CalculationRun` — see Tech Stack callout above. Also new: `ZoneOverview.razor` at `/irrigation/zones/{id}` — a landing page between the Zone List and the calculator flow (name/status/field/crop/area/last-watered, Edit shortcut, "Do calculations" entry, and a real link into Calculation History); Zone List cards now land here first instead of jumping straight into the calculator. Newest: **Zone Design** — `ZoneDesigns.razor` picker at `/irrigation/zone-designs` (the last dead NavMenu link, now real) and the `ZoneDesigner.razor` SVG layout designer at `/irrigation/zones/{id}/design` (draw pipes with drag/resize/reclassify, place + drag pump/sprinklers/valves/source with per-element overrides, live hydraulics + connectivity warnings + 3-mode solver reusing `Farm.Irrigation.Calculators`); designer logic lives in testable classes (`Farm.Web.Irrigation/Designer/ZoneDesignerSession` + `ZoneDesignEvaluator`, covered by `Farm.Web.Irrigation.Tests` — solution total 73 xunit tests: 49 calculators + 24 designer); designs are **backend-persisted as versioned saves** (`irrigation.zone_design_versions`, explicit Save/Discard/History with unsaved edits as a local playground) via `HttpZoneDesignStore` behind the `IZoneDesignStore` seam — see Tech Stack callout. Newest: **real per-zone `Schedule`** (`irrigation.schedules` + `ZoneSchedule.razor` editor at `/irrigation/zones/{id}/schedule` + Zone Overview card) — agronomic watering interval from soil/crop inputs via `SchedulingCalculators.MaxIrrigationInterval`, superseding the manual interval for `Zone.status` — see Tech Stack callout.
- **What's in progress:** —
- **Next concrete step:** Phase 1 infrastructure (Docker Compose over LAN, real-farm seed script, pg_dump backups) is the remaining definition-of-done work. Remaining Irrigation data model pieces: `WaterSource` and real `IrrigationSystem` config (still not built speculatively), and the weather-driven self-advancing water balance for `Schedule` (Phase 1.5 weather feed). Designer follow-up still open: pointer/touch events for tablet use. Worker role/views and multi-manager account creation UI are deferred (see plan.md Parking Lot).

---

## How to Work With This Repo (for AI assistants)

- Read `plan.md` first for the current task list and what's actively being worked on.
- Read this file for architecture rules and current stack — don't suggest a different stack/pattern without flagging it as a proposed change.
- When adding a module, follow the pattern in "Repo Structure" and "Architecture Rules" above.
- If you make an architectural decision (e.g. picking Prisma over Drizzle, adding a new module), update this file and `plan.md` in the same session — don't leave them stale.
